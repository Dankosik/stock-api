caching:
  specs:
    allShares:
      timeout: 2500000 #1 day
    figis:
      timeout: 10
reactive:
  feign:
    loadbalancer:
      enabled: false
    circuit:
      breaker:
        enabled: false
feign-services:
  moex-stock-api-endpoint: "http://localhost:${wiremock.server.port}"
  yahoo-stock-api-endpoint: "http://localhost:${wiremock.server.port}"